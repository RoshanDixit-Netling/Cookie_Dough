<!-- page head --->
      <div id="page-head" class="page-head2 text-center pt-2">
        <div class="container">
          <div class="image mb-4">
            <img src="{{ section.settings.image | image_url }}" alt="{{ section.settings.image.alt }}">
          </div>
          <div class="row justify-content-center text-md-start">
            <div class="col-xl-5 col-md-6">
              <h1 class="display-1">{{ section.settings.heading }}</h1>
            </div>
            <div class="col-xl-5 col-md-6">
              {{ section.settings.content }}
            </div>
          </div>
          <footer>
            <a href="#" class="btn btn-primary btn-xl">{{ section.settings.button-label }}</a>
          </footer>
        </div>
      </div>
<!-- /page head --->

{% schema %}
  {
    "name": "Banner",
    "settings": [
		{
			"id": "image",
			"type": "image_picker",
			"label": "Image"
		},
		{
			"id": "heading",
			"type": "text",
			"label": "Heading",
			"default": "Corporate gifting"
		},
		{
			"id": "content",
			"type": "richtext",
			"label": "Content",
			"default": "<p>We’ve got your back when it comes to your corporate gifting. Whether you need some of our indulgent bakes for employees working at home, hundreds of cookies for an event or regular cookie deliveries to your office.</p>"
		},
		{
			"id": "button-label",
			"type": "text",
			"label": "Button Label",
			"default": "Enquire Now"
		}
	],
	"presets": [
		{
			"name": "Banner"
		}
	]
  }
{% endschema %}