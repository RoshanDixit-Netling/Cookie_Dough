{%- paginate blog.articles by {{ section.settings.blogs-per-page }} -%}
      {% for article in blog.articles %}
      {% include 'news-card' %}
      {% endfor %}

    {%- if paginate.pages > 1 -%}
      {%- render 'pagination', paginate: paginate -%}
    {%- endif -%}
  </div>
{%- endpaginate -%}
        <div class="section news-overview wave1 bg-color1">
          <div class="container">
            <div class="row">
              {% for article in blog.articles %}
              {% include 'news-card' %}
              {% endfor %}
            </div>
            <nav aria-label="Page navigation">
              <ul class="pagination justify-content-center ">
                <li class="page-item prev"><a class="page-link" href="#">
                    < </a> </li> <li class="page-item"><span class="page-link active">1</span></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><span class="page-link or">...</span></li>
                <li class="page-item"><a class="page-link" href="#">23</a></li>
                <li class="page-item next"><a class="page-link" href="#"> > </a></li>
              </ul>
            </nav>
          </div>
        </div>
{% schema %}
{
  "name": "Main News Blog Section",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "number",
      "id": "blogs-per-page",
      "label": "Number of Blogs Per Page",
      "default": 6
    }
  ]
}
{% endschema %}