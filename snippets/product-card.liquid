{% comment %} <div class="col-xl-3 col-lg-4 col-md-6 column single-product">
<div class="figure1 video">
{%- for media in product.media limit: 1 -%}
{%- case media.media_type -%}
{%- when 'video' -%}
{{ media | video_tag: controls: false, autoplay: true, loop: true }}
{%- endcase -%}
{%- endfor -%}
<span class="hover">
<form
action="/cart/add"
method="post"
id="product-form- {{ product.id }}">
<input
type="hidden"
name="id"
value="{{ product.variants.first.id }}">
<div>
<button
type="submit"
class="btn btn-outline-primary bg-color1 btn-sm"
name="add">Add to cart</button>
</div>
</form>
{% endcomment %}
{%- comment -%} <a href="#" class="btn btn-outline-primary bg-color1 btn-sm">Add to cart</a> {%- endcomment -%}

{% comment %}  </span>
</div>
<div class="content">
<h4>
<a href="{{ product.url }}">{{ product.title }}</a>
</h4>
<p class="price">{{ product.price | money }}</p>
</div>
</div> {% endcomment %}<div class="col-xl-3 col-lg-4 col-md-6 column single-product"> <div class="figure1 video">
    {%- if product.featured_media.media_type == "video" -%}
      <img
        class="w-100 video-overlay"
        src="{{ product.featured_media | img_url: 'master' }}"
        alt="img">
      {{ product.featured_media | video_tag: controls: false, autoplay: true, loop: true }}
    {%- else if product.featured_media.media_type == "image" -%}
      <img
        class="w-100 video-overlay"
        src="{{ product.featured_media | img_url: '500x500' }}"
        alt="img">
    {%- endif -%}
    <span class="hover">
      <form
        action="/cart/add"
        method="post"
        id="product-form- {{ product.id }}">
        <input
          type="hidden"
          name="id"
          value="{{ product.variants.first.id }}">
        <div>
          <button
            type="submit"
            class="btn btn-outline-primary bg-color1 btn-sm"
            name="add">Add to cart</button>
        </div>
      </form>
      {% comment %} <a href="#" class="btn btn-outline-primary bg-color1 btn-sm">Add to cart</a> {% endcomment %}
    </span>
  </div>
  <div class="content">
    <h4>
      <a href="{{ product.url }}">{{ product.title }}</a>
    </h4>
    <p class="price">{{ product.price | money }}</p>
  </div>
</div>