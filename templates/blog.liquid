<!-- page head --->
<div id="page-head" class="page-head2 bg-gradient1 text-center pb-5 pb-lg-7">
  <div class="container">
    <h1 class="display-1">{{ blog.title }}</h1>
  </div>
</div>
<!-- /page head --->
<!-- content -->
<div id="content">
  <!-- section -->
  {%- capture section_capture -%}
    {%- section 'section-blogs' -%}
  {%- endcapture -%}
  {{ section_capture | replace: 'class="shopify-section section"', '' }}
  <!-- /section -->
</div>
<!-- /content -->